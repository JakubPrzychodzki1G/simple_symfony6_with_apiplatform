{% extends "base.html.twig" %}

{% block body %}
<div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4">
    {% if postPosts|length>0 %}
        {% for postPost in postPosts %}
            {{ include('post/_post.html.twig', {post:postPost})}}
        {% endfor %}
    {% else %}
        There's nothing here yet!
    {% endif %}
{% endblock %}